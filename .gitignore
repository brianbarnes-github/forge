build/
build-*/
.cache/
*.user

# Editors
.vscode/
.idea/
*.swp

# WSL/Windows artifacts
*:Zone.Identifier

# Git worktrees (feature-branch isolation workspaces)
.worktrees/

# Personal porting scratch
transport/
transport.zip

# Generated ABC output (converter runs produce these; not project artifacts)
*.abc

# User-dropped test MIDIs (personal scratch; tracked fixtures remain tracked)
midi/*.mid
